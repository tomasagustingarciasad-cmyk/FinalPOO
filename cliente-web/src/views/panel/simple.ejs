<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel de Control</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .container { max-width: 800px; }
        .btn { padding: 10px 20px; background: #007bff; color: white; text-decoration: none; border-radius: 5px; }
        .success { color: green; }
        .error { color: red; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Panel de Control del Robot</h1>
        <% if (user) { %>
            <p>Bienvenido, <strong><%= user.username %></strong> (Rol: <%= user.role %>)</p>
        <% } %>
    </div>
    
    <div class="container">
        <h2>Estado del Sistema</h2>
        
        <% if (error) { %>
            <div class="error">Error: <%= error %></div>
        <% } %>
        
        <% if (status) { %>
            <div class="success">
                <p><strong>Estado:</strong> Conectado</p>
                <p><strong>Modo:</strong> <%= status.mode %></p>
                <p><strong>Información:</strong> <%= status.info %></p>
            </div>
        <% } %>
        
        <% if (user && user.role === 'ADMIN') { %>
            <div style="margin-top: 30px;">
                <h3>Administración</h3>
                <a href="/panel/users" class="btn">Gestión de Usuarios</a>
            </div>
        <% } %>
        
        <div style="margin-top: 30px;">
            <h3>Control del Robot</h3>
            <a href="/robot/move" class="btn">Mover Robot</a>
            <a href="/robot/status" class="btn">Estado del Robot</a>
        </div>
    </div>
</body>
</html>